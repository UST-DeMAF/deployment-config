apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: C:\Windows\system32\kompose.exe convert
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: terraformplugin
  name: terraformplugin
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: terraformplugin
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: C:\Windows\system32\kompose.exe convert
        kompose.version: 1.34.0 (cbf2835db)
      labels:
        io.kompose.service: terraformplugin
    spec:
      containers:
        - env:
            - name: ANALYSIS_MANAGER_PLUGIN_REGISTRATION_URL
              value: http://analysismanager:8080/plugin-registration
            - name: MODELS_SERVICE_URL
              value: http://modelsservice:8081
            - name: RUNPROVIDERPOSTPROCESSORS
              value: "true"
            - name: SPRING_RABBITMQ_HOST
              value: rabbitmq
          image: ghcr.io/ust-demaf/terraform-mps-plugin:latest
          livenessProbe:
            exec:
              command:
                - curl -fs http://localhost:8084/actuator/health | grep 'UP' || exit 1
            failureThreshold: 3
            periodSeconds: 10
            timeoutSeconds: 5
          name: terraformplugin
          ports:
            - containerPort: 8084
              protocol: TCP
          volumeMounts:
            - mountPath: /usr/share
              name: demaf
      hostname: terraformplugin
      restartPolicy: Always
      volumes:
        - name: demaf
          persistentVolumeClaim:
            claimName: demaf
